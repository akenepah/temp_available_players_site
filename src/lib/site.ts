/**
 * Static page chrome for this microsite. Pool facts (counts, dates,
 * commissioner selection, players) come from the snapshot, never from here.
 */
export const SITE = {
  draftSeason: "2026-27",
  statsSeason: "2025-26",
  /**
   * The approved hero player collage (Celebrini / McKenna / Hutson with
   * "Scouting the pool") has not been supplied as a standalone asset. Set this
   * to its public path, e.g. "/brand/player-registry-hero.png", once added.
   */
  heroCollageSrc: null as string | null,
};
