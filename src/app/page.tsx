import { AvailablePlayersPage } from "@/components/AvailablePlayersPage";

/** Public, unauthenticated Available Players registry — the canonical experience for this site. */
export default function Home() {
  return <AvailablePlayersPage />;
}
